---
description: Harness loop - machine-verified done, no self-verify.
alwaysApply: true
---

<!-- harness-stock: true -->

Before implementing: exactly one `active` task (`node scripts/harness-start-task.mjs` if none). Before claiming done: read `docs/harness/LOOP.md`. Run the verify wrapper from `docs/harness/manifest.json`. Never set task status to verified by hand. Commit only if the user asked. Stop and ask before actions listed in the manifest `irreversibleActions`.
